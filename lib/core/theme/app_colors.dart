import 'package:flutter/material.dart';

class AppColors {
  // Cores primárias (inspiradas em pole dance - roxo/rosa)
  static const Color primary = Color(0xFF8B1E7D); // Roxo
  static const Color primaryLight = Color(0xFFB94FA0);
  static const Color primaryDark = Color(0xFF5F0F54);

  static const Color secondary = Color(0xFFFF6B9D); // Rosa
  static const Color secondaryLight = Color(0xFFFF9BBF);
  static const Color secondaryDark = Color(0xFFD4467B);

  // Cores de fundo
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Colors.white;

  // Cores de texto
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);

  // Cores de status
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFF44336);
  static const Color warning = Color(0xFFFF9800);
  static const Color info = Color(0xFF2196F3);

  // Cores neutras
  static const Color grey = Color(0xFF9E9E9E);
  static const Color greyLight = Color(0xFFE0E0E0);
  static const Color greyDark = Color(0xFF616161);
}
